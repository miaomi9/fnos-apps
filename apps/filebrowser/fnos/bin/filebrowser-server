#!/bin/sh
APP_DIR="${TRIM_APPDEST}"
APP_DATA_DIR=$1
DATA_DIR="$APP_DATA_DIR/data"

mkdir -p "$DATA_DIR/files"

cd "$APP_DIR" || exit 1
exec ./filebrowser --address 0.0.0.0 --port "${TRIM_SERVICE_PORT}" --database "$DATA_DIR/filebrowser.db" --root "$DATA_DIR/files"
